import { css } from 'styled-components'
import tw from 'twin.macro'

export const CheckCss = css`
  ${tw`border-2 border-gray-300 rounded text-base focus:border-black focus:ring focus:ring-offset-0 focus:ring-gray-400 focus:ring-opacity-50`}
`
